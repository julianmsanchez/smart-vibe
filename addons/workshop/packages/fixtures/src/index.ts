/**
 * Re-export raíz de fixtures (seeds + mocks MSW).
 *
 * Subpaths recomendados:
 * - `@workshop/fixtures/seeds`
 * - `@workshop/fixtures/msw-handlers`
 */

export * from '../seeds';
export * from '../msw-handlers';
