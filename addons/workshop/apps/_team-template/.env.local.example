# Variables de entorno team-specific.
#
# Copiar a `apps/<team>/.env.local` (gitignored).
#
# El bloque AUTO-GENERATED debajo se sincroniza con `workshop.yaml.
# shared_infra.apis_external[]` donde `access:` incluya este team-id,
# corriendo `bash scripts/sync-env.sh` desde la raíz del workshop.
#
# Si el team no necesita ninguna var propia, el bloque queda vacío
# (declarativo: "este team es 100% global").

# --- BEGIN AUTO-GENERATED FROM workshop.yaml ---
# NO editar a mano: regenerar con `bash scripts/sync-env.sh`.
# Origen: workshop.yaml.shared_infra.apis_external[] con access incluyendo este team-id.
# --- END AUTO-GENERATED FROM workshop.yaml ---
